/**
 *  Entrance of services
 *
 *  @author  jiang.liu
 *  @date    Jul 2, 2015
 *
 */
(function(define) {
    'use strict';

    define([
        './Events',
        './Utils'
    ], function() {
        return [].slice.call(arguments, 0);
    });

}(define));
